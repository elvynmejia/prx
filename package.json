{
  "name": "ontrail-frontend-proxy",
  "version": "1.0.0",
  "description": "A proxy middleware app ",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Elvyn M",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "http-proxy-middleware": "^1.0.6"
  }
}
